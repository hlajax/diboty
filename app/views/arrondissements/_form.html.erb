<%= form_with(model: arrondissement, local: true) do |form| %>
  <% if arrondissement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(arrondissement.errors.count, "error") %> prohibited this arrondissement from being saved:</h2>

      <ul>
      <% arrondissement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
