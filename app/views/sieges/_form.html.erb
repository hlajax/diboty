<%= form_with(model: siege, local: true) do |form| %>
  <% if siege.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(siege.errors.count, "error") %> prohibited this siege from being saved:</h2>

      <ul>
      <% siege.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
