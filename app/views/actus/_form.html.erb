<%= form_with(model: actu, local: true) do |form| %>
  <% if actu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actu.errors.count, "error") %> prohibited this actu from being saved:</h2>

      <ul>
      <% actu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
