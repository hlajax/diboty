<%= form_with(model: piece_locataire, local: true) do |form| %>
  <% if piece_locataire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(piece_locataire.errors.count, "error") %> prohibited this piece_locataire from being saved:</h2>

      <ul>
      <% piece_locataire.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
