<%= form_with(model: actualite, local: true) do |form| %>
  <% if actualite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(actualite.errors.count, "error") %> prohibited this actualite from being saved:</h2>

      <ul>
      <% actualite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titre %>
    <%= form.text_field :titre %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.file_field :photo %>
  </div>

  <div class="field">
    <%= form.label :contenu %>
    <%= form.text_area :contenu %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
