<%= form_with(model: forme, local: true) do |form| %>
  <% if forme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(forme.errors.count, "error") %> prohibited this forme from being saved:</h2>

      <ul>
      <% forme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
